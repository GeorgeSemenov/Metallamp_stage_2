mixin color-and-type-screen__heads-column(modifier)
  if modifier["heads"] == undefined
    - modifier["heads"] = [{header: "empy_head", modifier:''}];
  .color-and-type-screen__heads-column&attributes(attributes)
    each head in modifier.heads
      +color-and-type-screen__head-container(head)(class=head.modifier)